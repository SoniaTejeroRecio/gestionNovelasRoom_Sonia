package com.example.gestionnovelas.ui.theme
/*
//para interactuar con el repositorio


import android.app.Application
import androidx.lifecycle.AndroidViewModel
import com.example.gestionnovelas.ui.theme.NovelRepository
import com.example.gestionnovelas.ui.theme.Novel

class NovelViewModel(application: Application) : AndroidViewModel(application) {
    private val repository = NovelRepository(application)

    fun addNovel(novel: Novel) {
        repository.addNovel(novel)
    }
}

 */